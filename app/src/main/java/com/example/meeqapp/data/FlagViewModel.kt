package com.example.meeqapp.data


/*
class FlagViewModel(flagStore: FlagStore) : ViewModel() {


    val hasSeenPredictionOnboardingFlow: Flow<Boolean> =
        flagStore.getFlag(Flag.HAS_SEEN_PREDICTION_ONBOARDING, false)

    //fun setHasBeenSurveyedTrue(){flagStore.setTrue(Flag.HAS_BEEN_SURVEYED)}
}
*/

/*
class FlagViewModel (application: Application) : AndroidViewModel(application) {

    */
/*private val dataStore = DataStoreManager(application)

    val getFlag = dataStore.getFlag().asLiveData(Dispatchers.IO)

    fun setFlag(isDarkMode : Boolean) {
        viewModelScope.launch(Dispatchers.IO) {
            dataStore.setFlag(isDarkMode)
        }
    }*//*

}*/
