package com.example.meeqapp.ui.onboarding

/*
@Composable
fun ConditionScreen() {
    var slugs by remember { mutableStateOf(emptyList<String>()) }

    Column(
        modifier = Modifier
            .fillMaxSize()
            .padding(top = 24.dp + statusBarHeight, backgroundColor = theme.lightOffwhite)
    ) {
        ScrollView {
            MediumHeader(
                modifier = Modifier
                    .padding(bottom = 24.dp),
                text = { Text("Do you struggle with any of the following?") }
            )

            RoundedSelectorButton(
                title = "Fear",
                onPress = { onSelect("fear", slugs) },
                selected = slugs.contains("fear")
            )
            RoundedSelectorButton(
                title = "Sadness",
                onPress = { onSelect("sadness", slugs) },
                selected = slugs.contains("sadness")
            )
            RoundedSelectorButton(
                title = "Anxiety",
                onPress = { onSelect("anxiety", slugs) },
                selected = slugs.contains("anxiety")
            )
            RoundedSelectorButton(
                title = "Anger",
                onPress = { onSelect("anger", slugs) },
                selected = slugs.contains("anger")
            )
            RoundedSelectorButton(
                title = "Depression",
                onPress = { onSelect("depression", slugs) },
                selected = slugs.contains("depression")
            )
            RoundedSelectorButton(
                title = "Another Issue",
                onPress = { onSelect("other", slugs) },
                selected = slugs.contains("other")
            )

            Row(
                modifier = Modifier
                    .padding(top = 24.dp),
                horizontalArrangement = Arrangement.Center
            ) {
                ActionButton(
                    title = "Next",
                    width = "100%",
                    onPress = { onNext() }
                )
            }
        }
    }
}
*/

/*private fun onSelect(slug: String, selectedSlugs: List<String>) {
    // Update the list of selected slugs
    slugs = if (selectedSlugs.contains(slug)) {
        selectedSlugs - slug
    } else {
        selectedSlugs + slug
    }
}*/

