package com.example.meeqapp.ui.feedback

/*

@Composable
fun LeaveAReview() {
    // Assuming you have a Compose function called `Container`, `StatusBar`, `ScrollView`, `MediumHeader`, `HintHeader`,
// `Row`, `GhostButton`, and the necessary theme defined.

    Container(
        modifier = Modifier
            .padding(top = 24.dp + statusBarHeight)
            .background(Theme.colorLightOffwhite)
            .fillMaxSize()
    ) {
        StatusBar(
            barStyle = StatusBarStyle.DarkContent,
            hidden = false
        )

        ScrollView(
            modifier = Modifier
                .padding(bottom = 24.dp)
        ) {
            Column(
                modifier = Modifier
                    .padding(16.dp)
            ) {
                MediumHeader(
                    text = "Would you mind leaving Quirk a review?",
                    modifier = Modifier
                        .margin(bottom = 12.dp)
                )

                HintHeader(
                    text = "It's not something that we like bugging you with, but the structure of the Play Store forces us to. By reviewing Quirk, you help a lot more people find accessible help to their problems.",
                    modifier = Modifier
                        .margin(bottom = 24.dp)
                )

                Row(
                    modifier = Modifier
                        .padding(top = 24.dp)
                        .fillMaxWidth()
                        .wrapContentSize(align = Alignment.End)
                ) {
                    GhostButton(
                        title = "Sure! 👍",
                        onClick = { onLeaveReview() },
                        width = Modifier
                            .fillMaxWidth()
                    )
                }

                Row(
                    modifier = Modifier
                        .padding(top = 12.dp)
                        .fillMaxWidth()
                        .wrapContentSize(align = Alignment.End)
                ) {
                    GhostButton(
                        title = "No Thanks.",
                        onClick = { continue() },
                        width = Modifier
                            .fillMaxWidth()
                    )
                }
            }
        }
    }

}*/
