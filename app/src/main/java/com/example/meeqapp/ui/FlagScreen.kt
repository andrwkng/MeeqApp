package com.example.meeqapp.ui


/*@Composable
fun FlagExampleScreen(flagViewModel: UserPreferenceViewModel = viewModel()) {
    val hasSeenPredictionOnboarding by flagViewModel.hasSeenPredictionOnboardingFlow.collectAsState(initial = false)

    Column {
        Text(text = "Has Seen Prediction Onboarding: $hasSeenPredictionOnboarding")

        Button(onClick = {
            // Read the flag value on button click
            // You can use the value as needed
            // For demonstration purposes, we just print it here
            println("Flag Value: $hasSeenPredictionOnboarding")
        }) {
            Text(text = "Read Flag Value")
        }
    }
}*/

/*@Composable
fun FeatherAndroidTasksThemeExample() {
    // A surface container using the 'background' color from the theme
    Surface(
        modifier = Modifier.fillMaxSize(),
        color = MaterialTheme.colorScheme.background
    ) {
        FlagExampleScreen()
    }
}*/
