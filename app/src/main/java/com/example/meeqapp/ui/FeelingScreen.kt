package com.example.meeqapp.ui

/*
@Composable
fun  FeelingScreen() {
    Column(
        modifier = Modifier
            .fillMaxSize()
            .background(Theme.colorLightOffwhite)
            .padding(top = statusBarHeight + 24.dp),
        horizontalAlignment = Alignment.CenterHorizontally,
        verticalArrangement = Arrangement.Center
    ) {
        // StatusBar
        AndroidView(
            modifier = Modifier.fillMaxWidth(),
            factory = { context ->
                StatusBar(context).apply {
                    setBackgroundColor(Theme.colorLightOffwhite)
                    setStatusBarStyle(StatusBarStyle.DarkContent)
                }
            },
            update = {}
        )

        // MediumHeader
        MediumHeader(
            title = "How are you feeling now?",
            style = Modifier
                .padding(bottom = 24.dp)
                .fillMaxWidth()
                .align(Alignment.CenterHorizontally),
            //textAlign = TextAlign.Center,
        )

        // GhostButton - Felt Better
        GhostButton(
            title = "Better than before 👍",
            width = Modifier.fillMaxWidth(),
            borderColor = Theme.colorLightGray,
            textColor = Theme.colorDarkText,
            onClick = onFeltBetter()
        )

        // GhostButton - Felt The Same
        GhostButton(
            title = "About the same 🤷‍",
            width = Modifier.fillMaxWidth(),
            borderColor = Theme.colorLightGray,
            textColor = Theme.colorDarkText,
            onClick = onFeltTheSame()
        )

        // GhostButton - Felt Worse
        GhostButton(
            title = "Worse than before 👎",
            width = Modifier.fillMaxWidth(),
            borderColor = Theme.colorLightGray,
            textColor = Theme.colorDarkText,
            onClick = onFeltWorse()
        )
    }
}

fun onFeltWorse(): () -> Unit {

}

fun onFeltTheSame(): () -> Unit {

}

fun onFeltBetter(): () -> Unit {

}
*/
