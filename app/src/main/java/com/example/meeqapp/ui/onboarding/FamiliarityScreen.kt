package com.example.meeqapp.ui.onboarding

/*@Composable
fun FamiliarityScreen() {
    val slugs = remember { mutableStateOf(emptyList<String>()) }

    Column(
        modifier = Modifier
            .padding(top = 24.dp + statusBarHeight())
            //.background(MaterialTheme.colorScheme.background)
            .fillMaxSize()
            .padding(16.dp)
    ) {
        Text(
            text = "How familiar are you with CBT?",
            //style = MaterialTheme.typography.h5,
            modifier = Modifier.padding(bottom = 24.dp)
        )

        GhostButton(
            title = "Not familiar",
            //onClick = { onSelect("not-familiar") },
            //style = Modifier.padding(bottom = 12.dp)
        )

        GhostButton(
            title = "Somewhat familiar",
            onClick = { onSelect("somewhat-familiar") },
            style = Modifier.padding(bottom = 12.dp)
        )

        GhostButton(
            title = "Very familiar",
            onClick = { onSelect("very-familiar") },
            style = Modifier.padding(bottom = 12.dp)
        )
    }
}*/
/*
@Composable
fun onSelect(slug: String) {
    // Handle the selection logic here
}*/
