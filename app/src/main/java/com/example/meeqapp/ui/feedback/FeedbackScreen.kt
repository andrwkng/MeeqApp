package com.example.meeqapp.ui.feedback

/*
@Composable
fun FeedbackScreen() {
    Box(
        modifier = Modifier
            .fillMaxSize()
            .padding(top = 24.dp + statusBarHeight, bottom = 24.dp)
            .background(lightOffwhite)
    ) {
        // Status Bar
        StatusBar(
            barStyle = DarkContent,
            hidden = false
        )

        // Scroll View
        ScrollView(
            modifier = Modifier.padding(bottom = 24.dp)
        ) {
            // Content
            Column(
                modifier = Modifier.padding(horizontal = 16.dp)
            ) {
                // Medium Header
                MediumHeader(
                    text = "Just curious, anything Quirk could do better?",
                    modifier = Modifier
                        .padding(bottom = 12.dp, start = 0.dp)
                )

                // Hint Header
                HintHeader(
                    text = "Quirk is run by two brothers with deep personal experiences with mental health. " +
                            "But we don't know all the answers. We let you decide what we build, so if there's " +
                            "anything you'd like to change or a feature you'd like to see, please let us know!",
                    modifier = Modifier.padding(bottom = 24.dp)
                )

                // "🤔 I wish Quirk would..." Button
                GhostButton(
                    title = "🤔 I wish Quirk would...",
                    onClick = { onEmailFeedback() },
                    modifier = Modifier
                        .padding(top = 24.dp)
                        .fillMaxWidth()
                        .wrapContentWidth(Alignment.End)
                )

                // "❤️ I like Quirk!" Button
                GhostButton(
                    title = "❤️ I like Quirk!",
                    onClick = { onLeaveReview() },
                    modifier = Modifier
                        .padding(top = 12.dp)
                        .fillMaxWidth()
                        .wrapContentWidth(Alignment.End)
                )

                // "Continue without giving feedback" Button
                GhostButton(
                    title = "Continue without giving feedback",
                    onClick = { continue() },
                    modifier = Modifier
                        .padding(top = 12.dp)
                        .fillMaxWidth()
                        .wrapContentWidth(Alignment.End)
                )
            }
        }
    }

}*/
