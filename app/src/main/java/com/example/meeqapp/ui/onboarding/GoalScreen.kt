package com.example.meeqapp.ui.onboarding

/*

@Composable
fun GoalScreen() {
    val goal by remember { mutableStateOf(TextFieldValue()) }

    Column(
        modifier = Modifier
            .padding(top = 24.dp + statusBarHeight())
            .background(MaterialTheme.colorScheme.background)
            .fillMaxSize()
            .padding(16.dp),
        verticalArrangement = Arrangement.Top
    ) {
        Column(
            modifier = Modifier
                .weight(1f)
                .verticalScroll(rememberScrollState())
        ) {
            Text("What are your goals for Quirk?", style = MaterialTheme.typography.h5)

            Text(
                "This is just for you. We take your privacy extremely seriously. " +
                        "Your thoughts can never be read by a Quirk employee; your thoughts " +
                        "are only stored on this device.",
                style = MaterialTheme.typography.body2,
                color = MaterialTheme.colorScheme.onBackground.copy(alpha = 0.6f),
                modifier = Modifier.padding(vertical = 8.dp)
            )

            BasicTextField(
                value = goal,
                onValueChange = { goal = it },
                textStyle = MaterialTheme.typography.body1,
                modifier = Modifier
                    .fillMaxWidth()
                    .heightIn(min = 120.dp)
            )
        }

        Row(
            modifier = Modifier
                .fillMaxWidth()
                .padding(top = 24.dp),
            horizontalArrangement = Arrangement.Center
        ) {
            Button(
                onClick = { */
/* Handle next button click *//*
 },
                modifier = Modifier
                    .fillMaxWidth()
                    .height(56.dp)
            ) {
                Text("Next")
            }
        }
    }
}

@Composable
fun statusBarHeight(): Dp {
    // You may need to find the appropriate way to get the status bar height in Jetpack Compose
    return 24.dp
}
*/
