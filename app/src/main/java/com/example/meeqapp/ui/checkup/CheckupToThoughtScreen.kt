package com.example.meeqapp.ui.checkup

import androidx.compose.runtime.Composable

@Composable
fun CheckupToThoughtScreen(
    onChallenge: () -> Unit,
    onNoThanks: () -> Unit
) {
    /*Surface(
        modifier = Modifier
            .fillMaxSize()
            .background(MaterialTheme.colorScheme.background),
    ) {
        Column(
            modifier = Modifier
                .padding(24.dp)
                .fillMaxSize()
        ) {
            MediumHeader(text = "Let's work through it right now.")
            Spacer(modifier = Modifier.height(8.dp))
            HintHeader(
                text = "It's likely your thoughts are distorted. Practicing challenging your " +
                        "negative thoughts can help you feel a lot better."
            )
            Spacer(modifier = Modifier.height(12.dp))
            ActionButton(
                modifier = Modifier.fillMaxWidth(),
                onClick = onChallenge,
                text = "Okay, let's challenge it."
            )
            Spacer(modifier = Modifier.height(6.dp))
            GhostButton(
                modifier = Modifier.fillMaxWidth(),
                onClick = onNoThanks,
                text = "No thanks."
            )
        }
    }*/
}

