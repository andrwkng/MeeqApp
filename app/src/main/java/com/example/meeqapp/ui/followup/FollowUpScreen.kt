package com.example.meeqapp.ui.followup

/*

@Composable
fun FollowUpScreen() {
    Box(
        modifier = Modifier
            .fillMaxSize()
            .padding(top = statusBarHeight + 24.dp)
            .background(Theme.colorLightOffwhite)
            .padding(16.dp),
        contentAlignment = Alignment.Center
    ) {
        // Status Bar
        Spacer(
            modifier = Modifier
                .statusBarsHeight()
                //.background(MaterialTheme.colorBackground)
        )

        // Medium Header
        Text(
            text = "Want to follow up later?",
            modifier = Modifier
                .fillMaxWidth()
                .padding(bottom = 12.dp),
            style = MaterialTheme.typography.h5,
            textAlign = TextAlign.Center
        )

        // Hint Header
        Text(
            text = "This is a chance to re-examine your thoughts with a different " +
                    "perspective and cement your changed view.",
            modifier = Modifier
                .fillMaxWidth()
                .padding(bottom = 24.dp),
            style = MaterialTheme.typography.body1,
            textAlign = TextAlign.Center
        )

        // Action Button
        Button(
            onClick = { */
/* Handle Action Button Click *//*
 },
            modifier = Modifier
                .fillMaxWidth()
                .padding(bottom = 12.dp)
        ) {
            Text(text = "Sure, let's do it.")
        }

        // Ghost Button
        OutlinedButton(
            onClick = {
                // Handle Ghost Button Click
                // stats.userDidNotScheduleFollowUp()
                // onContinue()
            },
            modifier = Modifier
                .fillMaxWidth(),
            border = BorderStroke(1.dp, MaterialTheme.colorGray),
            colors = ButtonDefaults.outlinedButtonColors(
                contentColor = MaterialTheme.colorLightText
            )
        ) {
            Text(text = "No thanks.")
        }
    }

}*/
